meta {
  name: 4. View Ingested Schema
  type: http
  seq: 4
}

get {
  url: {{base_url}}/schema
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Returns all schema chunks currently loaded. Useful to verify ingestion worked before querying.
}
